<%- include('partials/header') %>
<%- include('partials/navbar') %>

<main class="error-page">
  <div class="container">
    <div class="row">
      <div class="col-lg-6 offset-lg-3">
        <div class="error-content text-center" data-aos="fade-up">
          <div class="error-image">
            <% if (typeof statusCode !== 'undefined' && statusCode === 404) { %>
              <img src="/img/error-404.svg" alt="Pagină negăsită" class="img-fluid">
            <% } else { %>
              <img src="/img/error-general.svg" alt="Eroare" class="img-fluid">
            <% } %>
          </div>
          
          <h1 class="error-title">
            <% if (typeof statusCode !== 'undefined' && statusCode === 404) { %>
              Pagină negăsită
            <% } else { %>
              A apărut o eroare
            <% } %>
          </h1>
          
          <p class="error-message">
            <% if (typeof mesaj !== 'undefined' && mesaj) { %>
              <%= mesaj %>
            <% } else if (typeof statusCode !== 'undefined' && statusCode === 404) { %>
              Pagina pe care o cauți nu există sau a fost mutată.
            <% } else { %>
              A apărut o eroare neașteptată. Te rugăm să încerci din nou mai târziu.
            <% } %>
          </p>
          
          <div class="error-actions">
            <a href="/" class="btn btn-primary">
              <i class="fas fa-home"></i> Înapoi la pagina principală
            </a>
            <a href="javascript:history.back()" class="btn btn-outline-secondary mt-2 mt-md-0 ms-md-2">
              <i class="fas fa-arrow-left"></i> Înapoi la pagina anterioară
            </a>
          </div>
          
          <div class="error-suggestions mt-5">
            <h3>Sugestii:</h3>
            <ul class="list-unstyled">
              <li><i class="fas fa-check-circle"></i> Verifică adresa URL pentru erori de scriere</li>
              <li><i class="fas fa-check-circle"></i> Folosește meniul de navigare pentru a găsi ceea ce cauți</li>
              <li><i class="fas fa-check-circle"></i> Întoarce-te la pagina principală și începe din nou</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
</main>

<style>
  .error-page {
    background-color: #f8f9fa;
    padding: 5rem 0;
    min-height: calc(100vh - var(--navbar-height) - var(--footer-height));
    display: flex;
    align-items: center;
  }
  
  .error-content {
    background-color: white;
    border-radius: 10px;
    padding: 3rem;
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
  }
  
  .error-image {
    max-width: 250px;
    margin: 0 auto 2rem;
  }
  
  .error-title {
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--primary-bg);
    margin-bottom: 1.5rem;
  }
  
  .error-message {
    font-size: 1.1rem;
    color: #6c757d;
    margin-bottom: 2rem;
  }
  
  .error-actions {
    margin-bottom: 2rem;
  }
  
  .error-suggestions {
    text-align: left;
    max-width: 300px;
    margin: 0 auto;
  }
  
  .error-suggestions h3 {
    font-size: 1.2rem;
    font-weight: 600;
    color: var(--primary-bg);
    margin-bottom: 1rem;
    text-align: center;
  }
  
  .error-suggestions li {
    color: #6c757d;
    margin-bottom: 0.5rem;
  }
  
  .error-suggestions li i {
    color: var(--accent-1);
    margin-right: 0.5rem;
  }
  
  @media (max-width: 767.98px) {
    .error-page {
      padding: 3rem 0;
    }
    
    .error-content {
      padding: 2rem;
    }
    
    .error-title {
      font-size: 2rem;
    }
    
    .error-image {
      max-width: 200px;
    }
  }
</style>

<%- include('partials/footer') %>